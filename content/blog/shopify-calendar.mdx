---
title: "Checklist de integraciones con Shopify y Google Calendar"
date: "2025-08-29"
summary: "Guía completa para conectar tu tienda Shopify con Google Calendar y automatizar la gestión de citas y eventos."
tags: ["shopify", "google-calendar", "integraciones", "ecommerce"]
---

## ¿Por qué integrar Shopify con Google Calendar?

La integración entre Shopify y Google Calendar permite automatizar la gestión de:

- **Citas de consultoría** post-compra
- **Sesiones de onboarding** para productos digitales
- **Entregas programadas** para servicios
- **Follow-ups automáticos** con clientes
- **Eventos de lanzamiento** y promociones

## Checklist de preparación

### ✅ Requisitos técnicos

- [ ] Tienda Shopify activa (cualquier plan)
- [ ] Cuenta Google Workspace o Gmail
- [ ] API de Google Calendar habilitada
- [ ] Webhooks de Shopify configurados
- [ ] Herramienta de integración (Zapier, n8n, o custom)

### ✅ Configuración en Shopify

- [ ] **Webhooks configurados** para:
  - `orders/create`
  - `orders/updated`
  - `orders/paid`
  - `orders/cancelled`

- [ ] **Metafields personalizados** para:
  - Tipo de cita requerida
  - Duración de la sesión
  - Zona horaria del cliente
  - Notas especiales

```javascript
// Ejemplo de metafield para productos
{
  "namespace": "calendar",
  "key": "appointment_type",
  "value": "consultation_60min",
  "type": "single_line_text_field"
}
```

### ✅ Configuración en Google Calendar

- [ ] **Calendarios separados** por tipo de evento:
  - Consultas de ventas
  - Onboarding de clientes
  - Soporte técnico
  - Entregas programadas

- [ ] **Configuración de disponibilidad**:
  - Horarios de trabajo definidos
  - Buffers entre citas (15-30 min)
  - Límites de citas por día
  - Días festivos bloqueados

- [ ] **Templates de eventos** configurados:
  - Título estándar
  - Descripción con datos del cliente
  - Ubicación (física o virtual)
  - Recordatorios automáticos

## Flujos de automatización

### 1. Compra → Cita automática

```
Shopify Order Created
↓
Validar producto requiere cita
↓
Buscar disponibilidad en Calendar
↓
Crear evento con datos del cliente
↓
Enviar confirmación por email
↓
Actualizar order notes en Shopify
```

### 2. Reagendamiento de citas

```
Cliente solicita cambio
↓
Validar nueva disponibilidad
↓
Actualizar evento en Calendar
↓
Notificar a todas las partes
↓
Actualizar registro en Shopify
```

### 3. Follow-up post-cita

```
Evento de Calendar completado
↓
Esperar 24h
↓
Enviar email de seguimiento
↓
Crear tarea de follow-up
↓
Actualizar customer tags en Shopify
```

## Configuración con n8n

### Nodos necesarios:

1. **Shopify Trigger**: escucha nuevos pedidos
2. **Function Node**: procesa datos del pedido
3. **Google Calendar**: busca disponibilidad
4. **Google Calendar**: crea evento
5. **Email Node**: envía confirmación
6. **Shopify Node**: actualiza pedido

### Ejemplo de función de procesamiento:

```javascript
// Procesar datos del pedido para Calendar
const order = $input.first().json;
const lineItems = order.line_items;

// Buscar productos que requieren cita
const appointmentItems = lineItems.filter((item) =>
  item.properties?.some(
    (prop) => prop.name === "requires_appointment" && prop.value === "true"
  )
);

if (appointmentItems.length > 0) {
  return {
    customer: {
      name: `${order.customer.first_name} ${order.customer.last_name}`,
      email: order.customer.email,
      phone: order.customer.phone,
      timezone: order.customer.default_address?.country_code || "ES",
    },
    appointment: {
      type: appointmentItems[0].properties.find(
        (p) => p.name === "appointment_type"
      )?.value,
      duration: 60, // minutos
      notes: `Pedido #${order.order_number} - ${appointmentItems.map((i) => i.title).join(", ")}`,
    },
    order_id: order.id,
  };
}

return null; // No requiere cita
```

## Configuración con Zapier

### Zaps recomendados:

1. **"Nueva compra → Crear cita"**
   - Trigger: New Order in Shopify
   - Filter: Product requires appointment
   - Action: Create Event in Google Calendar
   - Action: Send Email via Gmail

2. **"Cita completada → Follow-up"**
   - Trigger: Event Ended in Google Calendar
   - Delay: 1 day
   - Action: Send Follow-up Email
   - Action: Add Tag to Customer in Shopify

## Mejores prácticas

### 🎯 Experiencia del cliente

- **Confirmación inmediata**: email automático con detalles
- **Recordatorios múltiples**: 24h y 1h antes
- **Instrucciones claras**: cómo unirse, qué preparar
- **Reagendamiento fácil**: link directo en emails

### 🔧 Gestión operativa

- **Buffer time**: 15-30 min entre citas
- **Límites diarios**: máximo de citas por día
- **Backup calendar**: para emergencias
- **Reporting automático**: métricas semanales

### 📊 Métricas a trackear

- **Show-up rate**: % de clientes que asisten
- **Conversion rate**: citas → ventas adicionales
- **Satisfaction score**: rating post-cita
- **Rebooking rate**: clientes que reagendan

## Troubleshooting común

### Problemas de zona horaria

```javascript
// Convertir a zona horaria del cliente
const moment = require("moment-timezone");
const clientTimezone =
  order.customer.default_address?.country_code === "ES"
    ? "Europe/Madrid"
    : "UTC";
const appointmentTime = moment()
  .tz(clientTimezone)
  .add(2, "days")
  .hour(10)
  .minute(0);
```

### Conflictos de disponibilidad

- Implementar sistema de cola
- Ofrecer múltiples opciones
- Escalación automática a manager

### Cancelaciones de último momento

- Política clara de cancelación
- Automatización de reagendamiento
- Compensación automática (descuentos)

---

**¿Necesitas ayuda implementando estas integraciones?** En Synko configuramos todo el flujo automatizado entre Shopify y Google Calendar en menos de una semana.
